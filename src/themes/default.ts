import { ThemeOptions } from "@mui/material";

const primary = "#283593";

export const defaultTheme: ThemeOptions = {
  palette: {
    mode: "light",
    primary: {
      main: primary,
    },
  },
};
